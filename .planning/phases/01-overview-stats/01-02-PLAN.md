---
phase: 01-overview-stats
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/storyParts.ts
  - src/components/features/StoryPartsDisplay.tsx
  - src/app/dashboard/page.tsx
autonomous: true

must_haves:
  truths:
    - "ユーザーは3部構成（基礎編・葛藤編・統合編）の話数、期間、テーマを確認できる"
    - "各部のさくら回想回数が表示されている"
    - "既存のストーリータブに3部構成セクションが追加されている"
    - "データは正典(DASHBOARD.md)から正確に変換されている"
  artifacts:
    - path: "src/data/storyParts.ts"
      provides: "3部構成データ構造"
      exports: ["STORY_PARTS_DATA", "StoryPartData"]
    - path: "src/components/features/StoryPartsDisplay.tsx"
      provides: "3部構成表示コンポーネント"
      min_lines: 60
    - path: "src/app/dashboard/page.tsx"
      contains: "StoryPartsDisplayコンポーネントのインポートと使用"
  key_links:
    - from: "src/components/features/StoryPartsDisplay.tsx"
      to: "src/data/storyParts.ts"
      via: "import { STORY_PARTS_DATA }"
      pattern: "import.*STORY_PARTS_DATA"
    - from: "src/app/dashboard/page.tsx"
      to: "src/components/features/StoryPartsDisplay.tsx"
      via: "StorylineTab内でのJSXレンダリング"
      pattern: "<StoryPartsDisplay"
---

<objective>
3部構成データ構造を作成し、ダッシュボードのストーリータブに表示する。

Purpose: ユーザーが3部構成（基礎編・葛藤編・統合編）の基本情報を確認できるようにする
Output: src/data/storyParts.ts（新規）、src/components/features/StoryPartsDisplay.tsx（新規）、src/app/dashboard/page.tsx（変更）
</objective>

<execution_context>
@/Users/kitamuratatsuhiko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kitamuratatsuhiko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@/Users/kitamuratatsuhiko/UIanimated/src/app/dashboard/page.tsx
@/Users/kitamuratatsuhiko/UIanimated/src/data/episodes.ts
@/Users/kitamuratatsuhiko/UIanimated/novel/dashboards/DASHBOARD.md

# 既存データファイル
episodes.ts: 既にSTORY_PARTS定数がある（基礎編、葛藤編、統合編）
</context>

<tasks>

<task type="auto">
  <name>Enhance story parts data with detailed information</name>
  <files>src/data/storyParts.ts</files>
  <action>
src/data/storyParts.tsを新規作成し、3部構成の詳細データを定義する。

1. 型定義を作成（既存のepisodes.tsのStoryPartを拡張）：
```typescript
export interface StoryPartDetail {
  id: 'foundation' | 'conflict' | 'integration';
  label: string;
  episodes: string;
  range: [number, number];
  period: string; // 期間（例: 2026年4月〜10月）
  theme: string; // テーマ説明
  sakuraFlashbacks: number; // さくら回想回数
  sakuraFlashbackLimit: number; // 上限回数
  color: string; // グラデーション色
}

export interface SakuraFlashbackDistribution {
  foundation: number;
  conflict: number;
  integration: number;
  total: number;
}
```

2. 正典データ(DASHBOARD.mdの🏗️3部構成セクション)から値を抽出：
```typescript
export const STORY_PARTS_DATA: StoryPartDetail[] = [
  {
    id: 'foundation',
    label: '基礎編',
    episodes: '第1-40話',
    range: [1, 40],
    period: '2026年4月〜10月',
    theme: '陰陽五行の基礎・巡の再生と美咲との出会い',
    sakuraFlashbacks: 9,
    sakuraFlashbackLimit: 8,
    color: 'from-emerald-500 to-teal-600'
  },
  {
    id: 'conflict',
    label: '葛藤編',
    episodes: '第41-80話',
    range: [41, 80],
    period: '2026年11月〜2027年5月',
    theme: '巡×慧の対立・AI vs 運命の衝突',
    sakuraFlashbacks: 6,
    sakuraFlashbackLimit: 5,
    color: 'from-amber-500 to-orange-600'
  },
  {
    id: 'integration',
    label: '統合編',
    episodes: '第81-120話',
    range: [81, 120],
    period: '2027年6月〜12月',
    theme: '巡×慧×美咲の統合・新しい形の創造',
    sakuraFlashbacks: 5,
    sakuraFlashbackLimit: 4,
    color: 'from-violet-500 to-purple-600'
  }
];

export const SAKURA_DISTRIBUTION: SakuraFlashbackDistribution = {
  foundation: 9,
  conflict: 6,
  integration: 5,
  total: 20
};
```

3. 既存のepisodes.tsにあるSTORY_PARTSとの整合性を維持：
   - 既存のid, label, episodes, range, colorは同じ値を使用
   - period, theme, sakuraFlashbacksを追加して拡張

重要: 既存のcharacters.tsのデータパターン（型＋定数エクスポート）に従うこと。
  </action>
  <verify>
1. ファイルが作成されている: ls -la src/data/storyParts.ts
2. TypeScript型チェック: npx tsc --noEmit
3. 3部構成データが正典と一致することを確認（話数範囲、期間、回想回数）
  </verify>
  <done>
src/data/storyParts.tsが作成され、3部構成データ（基礎編1-40話・葛藤編41-80話・統合編81-120話）とさくら回想分布（9回・6回・5回）を正確に含んでいる
  </done>
</task>

<task type="auto">
  <name>Create StoryPartsDisplay component with cards layout</name>
  <files>src/components/features/StoryPartsDisplay.tsx</files>
  <action>
新規コンポーネント src/components/features/StoryPartsDisplay.tsx を作成する。

1. コンポーネント構造：
```typescript
interface StoryPartsDisplayProps {
  viewMode: 'simple' | 'detailed';
}

export function StoryPartsDisplay({ viewMode }: StoryPartsDisplayProps) {
  // 3部構成カードを縦積みレイアウトで表示
  // 各カードは部の情報を含む
}
```

2. 各部カードに含む情報：
   - 部ラベル（基礎編、葛藤編、統合編）
   - 話数範囲（第1-40話等）
   - 期間（2026年4月〜10月等）
   - テーマ説明
   - さくら回想回数と上限（例: 9回/上限8）
   - グラデーション背景（既存のSTORY_PARTSのcolorを使用）

3. レイアウト：
   - 縦積みのカードレイアウト（space-y-4）
   - 各カードはbg-slate-800/40、border border-slate-700/40、rounded-xl
   - Framer MotionのfadeIn + slideUpアニメーション（stagger: 0.1で順次表示）
   - カード内はflexまたはgridで情報を整理

4. さくら回想情報の表示：
   - バッジまたはタグ形式で回想回数を表示（例: "さくら回想 9回"）
   - 上限オーバーの場合は警告色（amber-400）で表示（例: "9回/上限8"）
   - viewModeがdetailedの場合は追加説明（"+1オーバー"）

5. 既存のStoryTimelineコンポーネントのスタイルパターンを参照：
   - 色使い方: from-emerald-500 to-teal-600 等
   - 卡片スタイル: bg-slate-800/40, border, rounded-xl
   - アニメーション: initial={{opacity: 0, y: 20}}, animate={{opacity: 1, y: 0}}

重要: CharacterCardのカードデザインパターン（gradient背景、icon、stats）に従うこと。
  </action>
  <verify>
1. コンポーネントが正常にインポートできる
2. 3つのカードが縦積みで表示される
3. 各カードに部の情報（ラベル、話数、期間、テーマ、回想回数）が正しく表示されている
4. グラデーション背景が各部の正しい色（基礎編は緑、葛藤編はオレンジ、統合編は紫）で表示されている
  </verify>
  <done>
StoryPartsDisplayコンポーネントが作成され、3部構成のカードを表示し、各部の話数、期間、テーマ、さくら回想回数を確認できる
  </done>
</task>

<task type="auto">
  <name>Integrate StoryPartsDisplay into storyline tab</name>
  <files>src/app/dashboard/page.tsx</files>
  <action>
src/app/dashboard/page.tsxを更新し、StoryPartsDisplayコンポーネントをストーリーラブに統合する。

1. インポートを追加：
```typescript
import { StoryPartsDisplay } from '@/components/features/StoryPartsDisplay';
import { STORY_PARTS_DATA } from '@/data/storyParts';
```

2. StorylineTab関数を更新：
   - 現在の<StoryTimeline viewMode={viewMode} />の前に<StoryPartsDisplay viewMode={viewMode} />を追加
   - セクション見出し<h2>を追加：「3部構成」
   - 既存のStoryTimelineは維持（ターニングポイントとの並存）

3. レイアウト構造：
```tsx
function StorylineTab({ viewMode }: { viewMode: ViewMode }) {
  return (
    <div className="space-y-6">
      <h2 className="text-lg font-semibold text-white">3部構成</h2>
      <StoryPartsDisplay viewMode={viewMode} />

      <h2 className="text-lg font-semibold text-white mt-8">ターニングポイント</h2>
      <StoryTimeline viewMode={viewMode} />
    </div>
  );
}
```

4. 既存のスタイルパターンを維持：
   - space-y-4をspace-y-6に変更（セクション間のスペース拡大）
   - motion.sectionやmotion.divのアニメーションは既存パターンを維持

5. 既存のStoryTimelineコンポーネントは変更なし：
   - ターニングポイント8件の表示は維持
   - 3部構成カードの後に配置して、全体像→詳細の流れを作る

重要: 既存のストーリーラブの機能と見た目を維持しつつ、新しい3部構成セクションを追加すること。
  </action>
  <verify>
1. ストーリータブを開き、「3部構成」セクションが表示されることを確認
2. 3つのカード（基礎編、葛藤編、統合編）が正しく表示されていることを確認
3. 各カードに話数、期間、テーマ、回想回数が含まれていることを確認
4. 既存のターニングポイントセクションが下に表示されていることを確認
5. TypeScriptエラーがないこと: npx tsc --noEmit
  </verify>
  <done>
ストーリーラブにStoryPartsDisplayコンポーネントが統合され、ユーザーは3部構成の基本情報（話数、期間、テーマ、さくら回想回数）を確認できる
  </done>
</task>

</tasks>

<verification>
After completing all tasks, verify the following:

1. **Data Accuracy**: src/data/storyParts.tsの値が正典と一致している
   - 基礎編: 第1-40話、2026年4月〜10月、回想9回
   - 葛藤編: 第41-80話、2026年11月〜2027年5月、回想6回
   - 統合編: 第81-120話、2027年6月〜12月、回想5回

2. **Component Display**: StoryPartsDisplayコンポーネントが3つのカードを正しく表示
   - 各カードにラベル、話数範囲、期間、テーマがある
   - さくら回想回数が表示されている
   - 上限オーバーの警告が表示されている

3. **Integration**: ストーリーラブの最上部に配置され、既存のStoryTimelineと調和している

4. **Visual Consistency**: カードの色使い方（緑、オレンジ、紫）が各部の正しい色である

5. **Type Safety**: TypeScript型エラーなし
</verification>

<success_criteria>
1. ユーザーは3部構成（基礎編・葛藤編・統合編）の話数、期間、テーマを確認できる
2. ユーザーはさくら回想シーンの全体分布（20回：基礎編9回、葛藤編6回、統合編5回）を確認できる
3. データは正典(DASHBOARD.md)から正確に取得されている
4. 既存のストーリータブのレイアウトとデザインの一貫性が維持されている
5. レスポンシブデザインでモバイルとデスクトップ両対応している
</success_criteria>

<output>
After completion, create `.planning/phases/01-overview-stats/01-02-SUMMARY.md` with:
- 3部構成カードの実装詳細
- さくら回想回数の表示方法
- 既存StoryTimelineとの統合方法
- Next steps (ready for 01-03)
</output>
